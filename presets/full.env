# プリセット: フル構成（規制業種の本番相当）
#
# 用途: 金融・公共・医療など、送信の FQDN 制御が求められる環境
# 所要時間: 30-40 分
# 概算コスト: 月 ~1,715 USD（インフラのみ）
#
# ⚠️ Azure Firewall が月 ~900 USD かかります。
#    検証後は必ず `azd down --force --purge` で削除してください。
#
# 使い方:
#   .\scripts\Set-Preset.ps1 -Preset full -EnvName ailz-full
#   cd infra-upstream ; azd up

AZURE_LOCATION=swedencentral

# ネットワーク: Zero Trust + 送信制御
NETWORK_ISOLATION=true
DEPLOY_AZURE_FIREWALL=true

# デプロイモード
DEPLOYMENT_MODE=standalone
GREEN_FIELD_DEPLOYMENT=true

# 運用アクセス
DEPLOY_VM=true
DEPLOY_JUMPBOX=true
DEPLOY_BASTION=true
DEPLOY_NAT_GATEWAY=true
DEPLOY_SOFTWARE=true
BASTION_SKU_NAME=Standard
BASTION_ENABLE_TUNNELING=true
AZURE_VM_SIZE=Standard_D2s_v3

# AI コンポーネント（フル）
DEPLOY_AAF_AGENT_SVC=true
DEPLOY_SEARCH_SERVICE=true
DEPLOY_MCP=true
DEPLOY_KEY_VAULT=true
DEPLOY_VM_KEY_VAULT=true
DEPLOY_LOG_ANALYTICS=true
DEPLOY_ACR_TASK_AGENT_POOL=true

# 監視の閉域化
ENABLE_PRIVATE_LOG_ANALYTICS=true

# オプション機能（必要に応じて有効化）
DEPLOY_GROUNDING_WITH_BING=false
DEPLOY_SPEECH_SERVICE=false
DEPLOY_POSTGRES=false
DEPLOY_HOSTED_AGENT=false

# 検索バックエンド
RETRIEVAL_BACKEND=foundry_iq
ENABLE_AGENTIC_RETRIEVAL=true
FOUNDRY_IQ_KNOWLEDGE_RETRIEVAL_BILLING_PLAN=free

# 命名
RESOURCE_NAMING_MODE=caf
CAF_ENVIRONMENT_NAME=prod
